<?php
/**
 * @var \Vendor\ProductInquiry\Block\ProductInquiry $block
 */
$product = $block->getCurrentProduct();
?>
<form action="<?= $escaper->escapeUrl($block->getUrl('productInquiry/index/index')) ?>" method="post">
<input type="hidden" name="form_key" value="<?= /* @noEscape */ $escaper->escapeHtml($block->getFormKey()) ?>" />
<input type="hidden" name="product_id" value="<?= /* @noEscape */ $escaper->escapeHtml($product->getId()) ?>" />

<input type="hidden" name="sku" value="<?= /* @noEscape */ $escaper->escapeHtml($product->getSku()) ?>" />
    
    <div>
        <label for="customer_name">Your Name:</label>
        <input type="text" name="customer_name" id="customer_name" data-validate='{"required":true}' />
    </div>

    <div>
        <label for="customer_email">Your Email:</label>
        <input type="email" name="customer_email" id="customer_email"  
               data-validate="{required:true, 'validate-email':true}" />
    </div>

    <div>
        <label for="inquiry_subject">Subject:</label>
        <input type="text" name="inquiry_subject" id="inquiry_subject" data-validate='{"required":true}' />
    </div>

    <div>
        <label for="inquiry_message">Message:</label>
        <textarea name="inquiry_message" id="inquiry_message" data-validate='{"required":true}'></textarea>
    </div>

    <div>
        <button type="submit">Submit Inquiry</button>
    </div>
</form>
